$('#item-' +<%= @item.id %>).hide();

// find ruby object and get the id, which is the same as in database

// html id => attribute on html tag
// didn't get that part

// item_id => variable, not an actual method call or other model that references id of another object

// why didn't this work
//  $('#item-' +<%= current_user.items.find([:id] %>).hide();
